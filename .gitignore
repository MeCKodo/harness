node_modules/
dist/
*.log
.DS_Store
pnpm-workspace.yaml
!examples/frontend-spa/pnpm-workspace.yaml
!examples/electron-app/pnpm-workspace.yaml

# Local design notes — kept out of the public repo.
/DESIGN.md

# Local real-repo regression fixtures — NEVER commit.
# Slimmed copies of internal repos, used only to check that harness-kit
# upgrades don't break real project shapes. Local-only, never pushed.
/fixtures/

# Local oh-my-codex orchestration state.
/.omx/

# Local release secrets & runbook — NEVER commit.
# .npmrc holds a live npmjs publish token; RELEASE.local.md is the runbook.
.npmrc
/RELEASE.local.md
